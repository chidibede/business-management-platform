import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'business-management-platform',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
